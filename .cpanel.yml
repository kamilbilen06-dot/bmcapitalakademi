---
deployment:
  tasks:
    - export DEPLOYPATH=/home/ucrjblb7bbh4/public_html
    - /usr/bin/rsync -a --exclude='.git' --exclude='api/*.local.php' --exclude='uploads/courses/**/*.mp4' --exclude='backups' --exclude='.tools' --exclude='.cursor' --exclude='*.zip' --exclude='*.rar' ./ $DEPLOYPATH/
